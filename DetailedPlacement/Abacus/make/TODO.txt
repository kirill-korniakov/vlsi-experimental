=================================================================
January 2007 TODO
=================================================================

BUGS
- остается проблема с выделением памяти. при попытке легализации мы падаем на new для 28
ряда. вероятно стоит освободить память после глобальной оптимизации. хотя task manager не пишет, 
что плейсер захватил много памяти - всего 80 метров. короче надо посмотреть.
--- решить проблему с мю0, почему-то встречается NAN, скорее всего из-за градиента
    что-то не так, слишком маленькое значение мю имеем
- если мю каждый раз близко к нулю, то размещение начинается с того, что все кластеры кладутся в центр платы, 
  а потом мучительно расталкиваются, те не происходит наследования координат с предыдущих этапов работы
  если это так, надо проверить, 
  1. вероятно стоит установить нижний порог на мю, чтобы не допускать стягивания кластеров
  2. вероятно способ вычисления начального мю неверен

CHECK
- биновая сетка: провести эксперименты с разным предпочтительным числом кластеров в бине
- перепроверить правильность вычисления потенциала и его градиента (Артем)
- проверить градиент полупериметра: в статье про анатомию формула (3)
- статические указатели в LogSumExpForClusters
- хранить координаты кластеров в одном массиве double'ов
- проверить возможность применения других методов аналитического решателя, 
  позволяющих накладывать условия на область принимаемых значений решения (-tao_blmvm)

PARAMETERS/EXPERIMENTS
- число сколько кластеров должно быть в бине? \ эти два вопроса смежные, они влияют на качество
- размер потенциала                           / "размазывания" элементов по плате
- альфа - параметр сглаживания в log-sum-exp
- параметры метода сопряженных градиентов
  - может стоит увеличить шаг в методе сопряженных градиентов?


IMPLEMENTATION
-- По умолчанию все скомпилировалось в Debug режиме и можно ставить точки останова в коде TAO и ходить по строкам. Для окончательных экспериментов необходимо получить библиотеки в Release конфигурации (может вообще интеловским компилятором).
- а APlace есть еще параметр, называемый эпсилон и соответствущий шагу МСГ,
  нам бы надо тоже такой ввести
- завести RunID (случайное число, генерируемое в функции main), 
  печатать его первым в имени файлов с размещением,
  метку времени удалить из названия. выглядеть это должно примерно так:
  ???ATLANT = informAtion Technologies Laboratory ANalyTical placer
  RunID_itlAnalyticalPlacer_STAGE_DESCRIPTION.pl - тогда все pl файлы одного запуска будут группироваться вместе
- избавиться от всех todo
  
  SPEEDUP
!!! оптимизировать кластеризацию - из-за нее невозможно отлаживаться в дебаге

REFACTORING
-- разнести текущий multilevel_framework по нескольким файлам:
   кластеризация, решение задачи НЛП - а может и по-другому как-нить
   сейчас навигация по файлам весьма затруднена
- слить вычисление пенальти и их градиента
- вычистить закомментированный код
- выкинуть генераторы случайных чисел
? удалить directedBinSwapping. Вероятно, что стоит провести с ним пару экспериментов
- itlDragon is still alive in our codes
- 

DONE

=================================================================
January 2007 DONE
=================================================================

BUGS
-- на стадии глобального размещения все элементы находятся ниже центра платы :: нужно было 
   в потенциалах домножить все на сигнум расстояния между кластером и бином

IMPLEMENTATION
+ реализовать внутренний цикл МСГ по различным мю
  
SPEEDUP

REFACTORING

NOT IMPORTANT
