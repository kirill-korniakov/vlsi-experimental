remoteclean: 

remote: 

.F.a: 
	-${FC} -c ${FFLAGS} ${FC_FLAGS} ${FCPPFLAGS} $<
	-${AR} ${AR_FLAGS} ${LIBNAME} $*.o
	-${RM} $*.o

.f.o .f90.o .f95.o: 
	-${FC} -c ${FFLAGS} ${FC_FLAGS} -o $@ $<

.f.a: 
	-${FC} -c ${FFLAGS} ${FC_FLAGS} $<
	-${AR} ${AR_FLAGS} ${LIBNAME} $*.o
	-${RM} $*.o

.F.o .F90.o .F95.o: 
	-${FC} -c ${FFLAGS} ${FC_FLAGS} ${FCPPFLAGS} -o $@ $<

libc: ${LIBNAME}(${OBJSC} ${SOBJSC})

libf: ${OBJSF}
	-${AR} ${AR_FLAGS} ${LIBNAME} ${OBJSF}

shared_arch: 

